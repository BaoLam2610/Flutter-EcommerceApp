import 'package:flutter/material.dart';

class BLTextField extends StatelessWidget {
  const BLTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return TextFormField(

    );
  }
}
